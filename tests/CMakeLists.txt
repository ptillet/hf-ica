foreach(PROG whiten)
    add_executable(${PROG}-test ${PROG}.cpp)
    target_link_libraries(${PROG}-test clica ${OPENCL_LIBRARIES})
    set_target_properties(${PROG}-test PROPERTIES COMPILE_FLAGS "-DVIENNACL_WITH_OPENCL")
    add_test(whiten whiten-test)
endforeach(PROG)
