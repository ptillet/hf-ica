foreach(F neo_ica)
        matlab_add_mex(NAME ${F}_mex SRC ${F}.cpp LINK_TO neo_ica)
        set_target_properties(${F}_mex PROPERTIES OUTPUT_NAME ${F})
        install(TARGETS ${F}_mex LIBRARY DESTINATION "${Matlab_ROOT_DIR}/toolbox/neo_ica/")
endforeach()
