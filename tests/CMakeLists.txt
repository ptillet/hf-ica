foreach(test whiten)
    add_executable(${test} ${test}.cpp)
    target_link_libraries(${test} ${OPENCL_LIBRARIES})
    set_target_properties(${test} PROPERTIES COMPILE_FLAGS "-DVIENNACL_WITH_OPENCL")
endforeach()
